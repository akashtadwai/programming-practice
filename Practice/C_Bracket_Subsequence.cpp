#include <bits/stdc++.h>
using namespace std;
typedef std::pair<int, int> ipair;
#define int long long
#define pb push_back
#define ff first
#define INF INT_MAX
#define ss second
#define fr(i, j, a) for (int i = j; i < a; i++)
#define cnt_ones(x) __builtin_popcount(x)
#define all(x) x.begin(), x.end()
#define vi vector<int>
#define mp make_pair
#define mod 1000000007
#define IOS                                                                    \
  std::ios::sync_with_stdio(false);                                            \
  cin.tie(NULL);                                                               \
  cout.tie(NULL);
void foo(x){
  cout<<x<<endl;
}
void init() {
  foo(4);
/*int n,k;cin>>n>>k;
string s;cin>>s;
stack<int>st;
vector<bool> index(n);
for(int i=0;i<n;i++){
    if(s[i]=='(')   {
        st.push(i);
    }
    else {
        int top=st.top();
        st.pop();
        index[top]=true;
        index[i]=true;
    }
}
fr(i,0,n)   if(index[i])    cout<<s[i];*/
}
int32_t main() {
  IOS;
  init();
  return 0;
}
